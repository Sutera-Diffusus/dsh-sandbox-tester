# DSH 测试沙盒插件(dsh-sandbox)设计文档

> 版本:v0.1(设计评审稿,含评审结论)
> 日期:2026-08-16
> 状态:评审通过,进入开发与验证

> **路径约定**:本文档中的绝对路径(`D:\DeepseekHarness_*`、`D:\ai-temp` 等)是作者开发环境的默认值示例;全部运行时路径均可在插件设置(settings 命名空间 `sandbox`)中修改,安装脚本支持 `--target` 指定任意 DSH 安装目录。

---

## 1. 背景与动机

### 1.1 问题

DSH 是插件化架构(cordis),**所有插件都运行在宿主进程内**。开发/测试类任务中:

- 待测插件的一个死循环、未捕获异常或内存泄漏,会直接导致**本体主进程卡死或崩溃**;
- 测试脚本的 bug(如误杀进程、误删数据)会直接作用于**本体数据**;
- 历史上已发生两次实质事故:
  - 2026-08-16 14:52:交互式 PowerShell 误杀本体 3080 进程 + 删除 `C:\Users\Suter\.dsh`,聊天记录/设置/插件注册全部丢失(见 MEMORY.md 政策 4);
  - 2026-08-16 16:40~20:21:迁移/修复期间的反复重启与 400,本体长期不可用。

### 1.2 现状(手工机制)

目前依赖人工纪律维持隔离,已有资产:

| 资产 | 位置 | 作用 |
|---|---|---|
| 桥接副本 | `D:\DeepseekHarness_Test`(端口 3181,home `D:\DeepseekHarness_Test_Data\.dsh`) | 测试/开发专用实例 |
| 启动守卫 | `D:\Deepseek harness\bin-guard.cjs` | 本体启动时校验 11 个关键文件,损坏自动从 `D:\DeepseekHarness_Backup` 回滚 |
| 恢复工具 | `D:\DeepseekHarness_WorkSpace\_dsh-recovery\` | watchdog / 健康检查 / 迁移脚本(一次性) |
| 长期记忆 | `D:\DeepseekHarness_WorkSpace\MEMORY.md` | 政策 1/4/5/6,靠 agent 自觉阅读执行 |

**痛点**:这些约束是"软约束"——依赖 agent 每次都先读 MEMORY.md 并自觉遵守,不可审计、不可强制执行。

### 1.3 目标

把政策 4/5/6 的规程**代码化**为插件:

1. 开发/测试任务默认路由到**完全隔离的沙盒实例**(独立进程 + 独立数据 + 独立端口);
2. 沙盒内任何崩溃/死循环/误操作**零影响本体**(本体 3080 全程可用);
3. 合回本体有**机器强制的门禁**(语法校验 + bundle 校验 + 备份 + 回滚清单),不通过就无法合回;
4. 用户可在设置页**可视化管理**沙盒生命周期。

---

## 2. 架构边界(重要)

### 2.1 插件无法沙盒化执行自己

- 插件运行在宿主进程内,**任何插件都无法在宿主进程内保护宿主进程**;
- 因此本插件不尝试"线程级/进程内沙箱"(架构上不可行,也是非目标);
- 真隔离的唯一边界是**操作系统进程**。

### 2.2 本插件的定位:沙盒编排器

```
dsh-sandbox(运行在本体进程内,只做编排,自身绝不执行危险代码)
 ├─ create   克隆本体 → 独立沙盒目录 + 独立 DSH_HOME + 自动分配端口
 ├─ inject   把待测插件/补丁打进沙盒(永远不碰本体 node_modules)
 ├─ run      预检(本体 3080 健康) → subprocess 拉起沙盒进程 → 健康检查
 ├─ verify   健康检查 + CDP 冒烟 + bundle 语法校验(沙盒进程内)
 ├─ report   产出结构化报告(通过/失败/日志)
 ├─ merge    门禁校验通过后才 apply+备份+回滚清单 合回本体
 ├─ backup   合回成功后自动 robocopy 同步 D:\DeepseekHarness_Backup(守卫回滚源保鲜)
 └─ destroy  整体销毁沙盒(磁盘回收)
```

**关键推论**:待测代码只进入沙盒进程;本插件自身是唯一跑在本体的新增代码,因此它的代码质量要求最高(见 §9 风险对策)。

---

## 3. 功能规格

### 3.1 原生工具(agent 在会话中直接调用)

| 工具 | 签名(草案) | 行为 |
|---|---|---|
| `sandbox_list` | `{}` | 列出所有沙盒:名称/状态(运行中·已停止·已销毁)/端口/home/年龄 |
| `sandbox_create` | `{ name?, basePort?, fullCopy? }` | 从本体程序目录复制出沙盒(**默认裁剪**:仅运行必需,排除 Launcher.exe/WebView2/backups/.npm-cache/运行态文件;`fullCopy: true` 时完整复制);创建独立 home;自动分配未占用端口(默认从 3182 起);写沙盒 cfg/启动脚本 |
| `sandbox_run` | `{ name, qa? }` | ①预检:确认本体 3080 监听未变、`D:\DeepseekHarness_Data\.dsh` 未被触碰;②subprocess 拉起沙盒(继承独立 env);③轮询健康检查直到 HTTP 200 或超时;④`qa: true` 时接跑一键 QA(CDP 9223 冒烟脚本,产出报告,见 §4.6) |
| `sandbox_inject` | `{ name, patch }` | 把待测补丁/插件应用到沙盒(apply 逻辑与 victorian 主题同模式:备份+写入+回滚清单) |
| `sandbox_health` | `{ name }` | 端口监听 / HTTP 200 / 关键 bundle 200 / 可选 CDP 冒烟入口 |
| `sandbox_stop` | `{ name }` | 停止沙盒进程树(仅沙盒 PID 树,与本体进程树严格区分) |
| `sandbox_destroy` | `{ name }` | stop + 删除沙盒目录与 home(有确认参数 `confirm` 防误删) |
| `sandbox_merge` | `{ name, targets }` | **门禁式合回**:仅允许白名单目标(本体补丁面 11 文件 + 明确列出的新增文件);前置强制校验,见 §5 |
| `sandbox_prune` | `{ dryRun? }` | 清理孤儿沙盒(进程已死且超龄的目录);**默认开启 48h 自动清理**:超过 48h 未运行的沙盒自动标记为待清理,`sandbox_list` 中提示,可一键 prune(周期可在设置中改) |

### 3.2 宿主服务(sandbox 域)

- 沙盒注册表:`$DSH_HOME/storages/sandbox-registry.json`(原子写,复用 dsh-atomic-write);
- 端口池管理:分配时用 `Get-NetTCPConnection` 探测,杜绝端口冲突;注册表防双开;
- 进程树追踪:记录每个沙盒的根 PID,stop 时按树终止(仅限沙盒树,禁杀本体树);
- 配置域:`settings.yaml` 下 `sandbox` 命名空间(根目录、端口起始、超时、保留策略)。

### 3.3 客户端 UI(设置页区段)

照抄 dsh-github v4/v5 验证过的模式(settings.section + HelpTip + 卡片):

- 设置 → 测试沙盒 区段:
  - 沙盒卡片列表:名称 / 状态徽标(运行中·停止·已销毁)/ **最近一次报告摘要徽标(通过/失败 + 失败摘录,来自 verify/QA/merge)** / 端口 / 年龄 / 操作按钮(启动·停止·健康检查·销毁);
  - "新建沙盒"表单(名称 + 可选端口 + 完整复制开关);
  - 合回门禁摘要卡(最近一次 merge 的校验结果);
  - 一键"清理孤儿沙盒"(含 48h 超龄标记)。

### 3.4 非目标(明确不做)

- ❌ 插件在宿主进程内的线程级隔离(架构不可行);
- ❌ 跨平台容器/VM 隔离(Windows 本机部署,进程级足够);
- ❌ 修改本体任何现有文件(插件以 bundle 形式接入,同 dsh-github;唯一的本体改动见 §4.1);
- ❌ 自动化"全量 CI"(本插件是交互式开发沙盒,不是 CI 平台)。

---

## 4. 技术方案

### 4.1 本体改动点(最小化,仅 1 行)

- 沿用 dsh-github v4 先例:`dsh-host-apiproxy` 的 `WEB_SETTINGS_NAMESPACES` 白名单增加 `sandbox`(先备份 `index.js.dsh-sandbox-bak`,运行中进程不受影响,重启后生效)。
- 除此之外本体零改动。

### 4.2 复用的 DSH 内部基础设施

| 包 | 用途 |
|---|---|
| `dsh-subprocess` / `dsh-subprocess-local` | 拉起/终止沙盒进程(进程外执行通道) |
| `dsh-atomic-write` | 沙盒注册表、报告文件的原子写 |
| `dsh-jobs` | 沙盒创建/销毁等长任务的 job 化(进度可查) |
| `dsh-windows-notify` | 沙盒任务完成 toast |
| `dsh-spill` / `dsh-spill-local` | 沙盒 stdout/stderr 日志暂存 |
| `dsh-settings` + settings 域 | 沙盒配置的 GUI 通道 |
| `dsh-host-directory-picker` | (可选)允许用户在 UI 选择沙盒根目录 |

### 4.3 沙盒结构(创建产物)

```
D:\DeepseekHarness_Sandboxes\<name>\
 ├─ program\                    ← 本体程序目录的完整复制(排除 node_modules 之外的可选裁剪)
 ├─ home\.dsh\                  ← 独立 DSH_HOME(全新初始化)
 ├─ start-sandbox.bat           ← 强制 set DSH_HOME=<sandbox home> + set DSH_INSTALL_DIR=<sandbox program> + 端口
 ├─ DeepSeekHarness-Launcher.cfg ← 指向自身路径与端口(参照桥接副本)
 └─ meta.json                   ← 创建时间/端口/根PID/来源本体版本/状态
```

### 4.4 端口与进程隔离

- 端口:自动分配,起始 3182(3181 保留给固定桥接副本),逐个探测 `Get-NetTCPConnection`;
- 进程:沙盒由 `node <sandbox-program>\node_modules\...\bin.js web --port <p>` 直接拉起(不经 bin-guard,守卫只服务本体);终止时按记录的根 PID 走进程树,并校验**树内不含本体 3080 进程**(防参数错杀);
- 数据:沙盒 env 强制 `DSH_HOME=<sandbox home>`,启动脚本是唯一入口(同政策 5:禁止直接双击沙盒 launcher)。

### 4.5 日志与报告

- 沙盒 stdout/stderr → `D:\ai-temp\dsh-sandbox-<name>.log`(同 bin-guard 日志位置惯例);
- 每次 verify 产出 `report.json`:健康检查项、CDP 冒烟项、bundle 校验项、结论(通过/失败)、失败摘录。

---

### 4.6 一键 QA(sandbox_run --qa)

- 复用 `_dsh-recovery`/dsh-github 沉淀的 CDP 工具链:headless Edge 9223 连接沙盒端口,跑预设冒烟(首页 200、设置页渲染、关键 bundle 200、无控制台异常);
- 产出 `report.json`(与 §4.5 同格式),结论回显到工具结果与 UI 卡片徽标;
- QA 脚本本身运行在**沙盒侧工具进程**(不经本体),QA 失败不影响本体。

## 5. 合回门禁(sandbox_merge 的强制校验,不可绕过)

按 MEMORY 政策 6 的"合回后强制验证"升级为**合回前强制验证**:

1. **目标白名单**:仅允许 11 个补丁面文件 + 明确声明的新增文件;拒绝写入其他任何本体路径;
2. **语法门禁**:对每个待合回文件 `node --check`(HTML 做结构校验);任一失败 → 拒绝合回;
3. **bundle 门禁**:从本体 3080 抓取受影响 bundle,比对/校验注册形态(参照 `_dsh-recovery` 的 check-*.js 手法);
4. **备份先行**:合回前把目标文件备份到 `D:\DeepseekHarness_Backup\merge-<ts>\`(与守卫回滚源同根,但分目录);
5. **回滚清单**:写出本次合回的 apply/rollback 脚本对;
6. **合回后守卫联动**:成功后自动 robocopy 同步 `D:\DeepseekHarness_Backup`(政策 6"已知良好备份维护"自动化);
7. **失败即停**:任何一步失败,立即从备份恢复,绝不带着坏状态收工。

---

## 6. 与现有机制的关系(不重复造轮子)

| 机制 | 关系 |
|---|---|
| `bin-guard.cjs`(本体启动守卫) | **互补**:守卫管"本体启动自愈";沙盒插件管"测试隔离 + 合回门禁"。沙盒 merge 成功后同步守卫回滚源 |
| 固定桥接副本 `D:\DeepseekHarness_Test`(3181) | **保留**:作为长期开发副本不变;沙盒插件用于临时性/一次性测试(用完即销毁),两者共存,沙盒起始端口避开 3181 |
| `_dsh-recovery` 脚本 | 一次性恢复工具,不迁移;其 check-*.js / health-check.mjs 手法吸收进 merge 门禁 |
| MEMORY.md 政策 1/4/5/6 | 本插件是政策的**可执行形态**;落地后新增政策 7:开发/测试任务默认路由到沙盒插件,手工副本流程退居二线 |

---

## 7. 分阶段实施计划

| 阶段 | 内容 | 环境 | 验收 |
|---|---|---|---|
| 0 | 设计评审(本文档) | 真实工作区(只读) | 用户批准 |
| 1 | 宿主插件(服务 + 工具 + 门禁逻辑) | **桥接副本**(3181)内开发 | 沙盒创建→注入→崩溃→本体无损,全链路工具可用 |
| 2 | 客户端 UI(设置页区段) | 桥接副本 + CDP 9223 验证 | 卡片渲染/操作/工具提示视觉评审 ≥7/10 |
| 3 | QA 与对抗测试 | 沙盒内 | 见 §8 验收标准 |
| 4 | 合回本体(经 merge 门禁)+ 用户重启 | 本体 | 设置页出现区段;工具注册 |
| 5 | MEMORY.md 政策 7 + 文档五件套 | 真实工作区 | 长期记忆更新 |

---

## 8. 验收标准

1. **隔离性**:沙盒内故意注入死循环/未捕获异常插件 → 沙盒进程崩溃,**本体 3080 全程 HTTP 200**,本体会话不中断;
2. **门禁**:伪造坏语法补丁请求 merge → 拒绝并自动恢复,本体零变化;
3. **可管理**:UI 卡片可完成 创建→启动→健康检查→停止→销毁 全流程;
4. **自清理**:`sandbox_prune` 能识别并清理孤儿沙盒;
5. **零负担**:无沙盒运行时,插件不产生任何轮询/后台开销;
6. **自身稳定**:插件在本体连续运行 24h 无未捕获异常、无事件循环阻塞(自身是最高风险点,单独观察)。

---

## 9. 风险与对策

| 风险 | 对策 |
|---|---|
| 插件自身 bug 拖垮本体(它在宿主进程内) | 全部 I/O 走异步;工具入口全 try/catch;复制/校验等重活放 job + subprocess;开发阶段先在桥接副本压测 |
| 复制本体时读到运行态不一致文件 | 复制只读源、跳过本体锁/日志/缓存目录;沙盒首次启动自检 |
| 端口冲突 | 分配前探测;注册表持久化;冲突时自动+1 重试 |
| 误杀本体进程 | 进程树终止前校验树内不含 3080 监听 PID;stop 工具双保险(端口→PID 反查) |
| 磁盘膨胀 | 沙盒整体可销毁;默认 48h 超龄自动标记 + prune 兜底;创建时统计磁盘余量 |
| 与桥接副本/守卫的端口或数据打架 | 端口段划分:本体 3080 / 固定副本 3181 / 沙盒 3182+;home 全部独立 |

---

## 10. 评审结论(2026-08-16 用户已拍板)

1. **沙盒程序目录**:默认裁剪(仅运行必需),`sandbox_create fullCopy: true` 可切完整复制;✅
2. **一键自动 QA**:内置,`sandbox_run qa: true` 跑 CDP 冒烟并出报告;✅
3. **自动清理**:默认开 48h 超龄标记,配合一键 prune(周期可在设置中改);✅
4. **UI 报告摘要**:沙盒卡片显示最近一次 verify/QA/merge 结论徽标与失败摘录;✅

以上四项已落入 §3.1 / §3.3 / §4.6 / §9 相应条款。设计评审通过,进入阶段 1(桥接副本 3181 内开发)。
